<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>

  <body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white m-auto">
      <div class="taskForm p-10">
        <form action="/create" method="post">
          <input
            class="block rounded outline-none px-5 py-2 bg-zinc-800 w-full border-solid border-2 border-blue-900 hover:border-blue-600 hover:duration-300 hover:ease-in-out"
            type="text"
            name="title"
            placeholder="Write Your Task Title Here"
          />
          <textarea
            class="block rounded mt-4 outline-none px-5 py-2 bg-zinc-800 resize-none w-full border-solid border-2 border-blue-900 hover:border-blue-600 hover:duration-300 hover:ease-in-out"
            placeholder="Give your task details here"
            name="description"
          ></textarea>
          <input
            class="mt-5 cursor-pointer bg-zinc-800 px-5 py-2 rounded border-solid border-2 border-blue-700 hover:border-blue-500 hover:duration-300 hover:ease-in-out"
            type="submit"
            value="Create"
          />
        </form>
      </div>
      <div class="task p-10 flex gap-5 flex-wrap">
        <% if(files.length > 0){ %> <% files.forEach(function(val){ %>
        <div class="task w-64 px-4 py-3 bg-zinc-700 rounded">
          <h1 class="text-lg tracking-tighter"><%= val %></h1>
          <div class="flex w-full justify-between item-center">
            <a class="text-blue-400 inline-block" href="/files/<%= val %>"
              >read more...</a
            >
            <a class="text-blue-400 inline-block" href="/edit/<%= val %>"
              >Edit</a
            >
            <a
              class="delete text-red-400 inline-block"
              href="/delete/<%= val %>"
              >Delete</a
            >
          </div>
        </div>

        <% }) %> <% } else{ %>
        <h2 class="text-4xl text-slate-400">No Task Yet !!!</h2>
        <% } %>
      </div>
    </div>
  </body>
</html>
